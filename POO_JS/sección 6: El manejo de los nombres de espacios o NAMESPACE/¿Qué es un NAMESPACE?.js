/**
 *  ¿Qué es un NAMESPACE?
 * Se debe de evitar tener variables glogales sin relación.
 * Para ello podemos crear un objeto y almacenar ahí las propiedades
 * y métodos globales.
 * A ese objeto lo llamaremos NAMESPACE y por convención se
 * escribe en mayúsculas.
 * Podemos hacer propiedades para agrupar eventos, texto, elementos (DOM), etc.
 */
